# Coral TPU Requirements
# Install these packages for Coral Dev Board deployment

# Core Coral TPU packages
pycoral>=2.0.0
tflite-runtime>=2.5.0

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=9.5.0

# Model conversion (for development machine)
ultralytics>=8.0.0
tensorflow>=2.13.0
onnx>=1.14.0
onnx2tf>=1.15.0

# Audio and utilities
subprocess32; python_version < "3.0"

# Additional utilities for development
matplotlib>=3.7.0
tqdm>=4.65.0

# Note: Install Edge TPU compiler separately:
# For Ubuntu/Debian:
# curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
# echo "deb https://packages.cloud.google.com/apt coral-edgetpu-stable main" | sudo tee /etc/apt/sources.list.d/coral-edgetpu.list
# sudo apt update && sudo apt install edgetpu-compiler

# For macOS (development only - no TPU runtime):
# The TPU compiler is not available for macOS, use Linux for compilation
